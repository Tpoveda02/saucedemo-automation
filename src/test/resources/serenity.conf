serenity {
    take.screenshots = FOR_FAILURES
    logging = VERBOSE
    encoding = "UTF-8"
    compress.filenames = true
}
webdriver {
  driver = edge
  autodownload = true
  capabilities {
    browserName = "MicrosoftEdge"
    "ms:edgeOptions" {
      args = ["--start-maximized"]
    }
  }
}